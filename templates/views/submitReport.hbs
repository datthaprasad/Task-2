<!DOCTYPE html>
<html>
    <body>
        <form action='/submitReport:{{this._id}}' method="POST">
            <h1> {{this.name}} report</h1>
            <label>Completed Hours today ? (*HH:MM format) </label>
            <input type="text" name="completed">
            <input type="submit" value="submit">
        </form>
    </body>
</html>